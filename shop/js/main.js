$(document).ready(function(){    $(".add-to-cart").on("click", function() {        var cart = $(".add-cart");        var imgtodrag = $(this).parent(".item").find("img").eq(0);        if (imgtodrag) {            var imgclone = imgtodrag                .clone()                .offset({                    top: imgtodrag.offset().top,                    left: imgtodrag.offset().left                })                .css({                    opacity: "0.5",                    position: "absolute",                    height: "150px",                    width: "150px",                    "z-index": "100"                })                .appendTo($("body"))                .animate(                    {                        top: cart.offset().top + 10,                        left: cart.offset().left + 10,                        width: 75,                        height: 75                    },                    1000                );            imgclone.animate(                {                    width: 0,                    height: 0                },                function() {                    $(this).detach();                }            );        }    });    $("#profile-button").on("click", function() {        $(".cotn_principal").css('display','block');    });    $(".cross-div > a").on("click", function() {        $(".cotn_principal").css('display','none');    });    $(function() {        // contact form animations        $('#mail_us').click(function() {            $('.contactForm').fadeToggle();        })        $(document).mouseup(function (e) {            var container = $(".contactForm");            if (!container.is(e.target) // if the target of the click isn't the container...                && container.has(e.target).length === 0) // ... nor a descendant of the container            {                container.fadeOut();            }        });    });    // $(".add-cart").on("click", function() {    //    window.open("shopping_card.html");    // });    $(".add").on("click", function() {       var add = $(".integer").val() ;       add++;       $(".integer").val(add);       var total = add * parseInt(parseInt($(".itemus").text()));        $(".total_of_item").text(total.toString());        $(".total_of_item").append("<span>руб</span>");        $(".total_price").text(total - ((total * parseInt($(".total_percent").text())/100)));        $(".total_price").append("<span>руб</span>");    });    $(".sub").on("click", function() {        var sub = $(".integer").val() ;        sub--;        if(sub <=0){            $(".integer").val(1);        }else {            $(".integer").val(sub);            var total = parseInt($(".total_of_item").text()) - (parseInt($(".itemus").text()));            $(".total_of_item").text(total.toString());            $(".total_of_item").append("<span>руб</span>");            $(".total_price").text(total - ((total * parseInt($(".total_percent").text())/100)));            $(".total_price").append("<span>руб</span>");        }    });    $( function() {        $( "#slider-range" ).slider({            range: true,            min: 0,            max: 60000,            values: [ 75, 10000 ],            slide: function( event, ui ) {                $( "#amount" ).val( ui.values[ 0 ] + " руб" + " - " + ui.values[ 1 ] + " руб" );            }        });        $( "#amount" ).val($( "#slider-range" ).slider( "values", 0 ) + " руб" +            " - " + $( "#slider-range" ).slider( "values", 1 ) + " руб");    } );});